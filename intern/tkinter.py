import tkinter
window=tkinter.Tk()
# to name the title of the window
window.title("GUI")
# pack is used to show the object in window
label=tkinter.Label(window,text="Hello work").pack()
window.mainloop()